FROM node:latest

WORKDIR ./home/source
COPY . .

RUN apt update && apt -y install nginx &&  \
    npm install pnpm && pnpm install && pnpm run build_host

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
FROM node:latest

WORKDIR /home/source
COPY . .


RUN apt update && apt -y install nginx &&  \
    npm install -g pnpm && pnpm run build_host && pnpm install typescript -g

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
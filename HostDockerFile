FROM node:latest

WORKDIR ./home/source
COPY . .

RUN npm install pnpm
RUN apt update && apt -y install nginx &&  \
    pnpm install && pnpm run build_host

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
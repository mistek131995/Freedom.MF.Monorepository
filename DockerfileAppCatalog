FROM node:latest

WORKDIR /home/source
COPY . .

RUN apt update && apt -y install nginx &&  \
    npm install -g pnpm && pnpm install && pnpm run build_app_catalog

RUN mv -f ./default /etc/nginx/sites-available

EXPOSE 81

CMD ["nginx", "-g", "daemon off;"]